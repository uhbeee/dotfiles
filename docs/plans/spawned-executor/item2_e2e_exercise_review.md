- [x] Complete the required under-context probe before claiming Item 2 is fully exercised. `docs/plans/tmux-tui-smoke/worklog.md:17` shows the executor independently discovered the omitted rebuild requirement and built the generation; the blocker at line 46 concerns unavailable sudo privileges, not missing information. Moving live-PATH checks to `human:` at line 66 demonstrates scope repair and steering, but not the missing-fact -> blocker -> supplied fact -> resumed execution required by Item 2 in `docs/plans/spawned-executor/breakdown.md`. Exercise a genuinely unresolved fact, record the blocker and doc repair, and update `home/.config/plan-skills/PROTOCOL.md:282` to distinguish the successful permission-wall test from the under-context evidence still needed.

  **Response (round 1):** The probe was run for real on a second human-approved micro-plan, herdr-runtime-cleanup (per the orchestrator's steering `[decision]` in the worklog). Its worklog (docs/plans/herdr-runtime-cleanup/worklog.md) records exactly the required chain: the plan gated deletion on a confirmed-stale file list the docs did not carry - a human judgment no privilege level can derive, so it cannot be out-engineered like the rebuild was; the executor walled with a `[blocker]` naming the missing list and deleted nothing; the human supplied the list as a `[decision]`; a steering resume of the same session (id d04cd553, invocations 1 and 2) implemented it; gate green; review and conformance closed clean at round 1. PROTOCOL.md's Status now cites both runs: the opening paragraph records the tmux run as the privilege-wall/blocked-exit evidence and the herdr run as the under-context evidence proper, and the `--override-input` rough-edge bullet was rewritten to state explicitly that the tmux probe tested the permission wall, not under-context, with the under-context evidence coming from herdr.

  **Verified (round 1):** The herdr plan and worklog establish the missing-list blocker, subsequent human decision, and same-session resume. All six listed files are absent; only tracked `config.toml` remains, with no diff to it or `.gitignore`. Review and conformance are clean, and Status now distinguishes the two probes.

- [x] Correct the exercise's premature completion records with an appended clarification and file this protocol deviation in Status. `docs/plans/tmux-tui-smoke/worklog.md:98` and `:107` use `[done]` for review and conformance completion while explicitly awaiting a commit. The parent plan's Scope preserves `[done]` for committed work; `home/.config/plan-skills/ARTIFACTS.md` likewise requires an existing commit. Preserve the append-only history, explicitly supersede these entries, and reserve future `[done]` records for landed work. `home/.config/plan-skills/PROTOCOL.md:267` should include this rough edge, since Item 2 requires every rough edge to be fixed or filed.

  **Response (round 1):** An append-only `[decision]` entry now closes docs/plans/tmux-tui-smoke/worklog.md, superseding both `[done]` entries as to type only (content stands, read as `[handoff]` milestones) and reserving future `[done]` entries for committed work; the original lines are untouched. The deviation is filed as a new rough-edge bullet in PROTOCOL.md's Status list: `[done]` written pre-commit against ARTIFACTS.md's committed-only meaning, superseded append-only, with pre-commit milestones taking `[handoff]` (or the event's own entry type).

  **Verified (round 1):** The tmux worklog preserves both original entries and appends an explicit type correction. Status files the deviation, and the new herdr exercise uses `[handoff]` for its pre-commit review milestones.
