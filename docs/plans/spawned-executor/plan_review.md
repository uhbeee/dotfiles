- [x] **Profile recording has no delivery path.** [plan.md:26](plan.md:26) requires executor and reviewer profiles to be recorded at plan-create time, but [breakdown.md:7](breakdown.md:7) only assigns changes to the implementation/review adapters and core sections that do not cover plan creation. The existing create adapters collect only a design-review profile ([Claude](../../../home/.claude/skills/plan-create/SKILL.md:14), [Codex](../../../home/.codex/prompts/plan-create.md:9)). Assign the creation-time recording requirement to an explicit work item and distinguish implementation reviewer selection from design reviewer selection. Define the fallback for existing approved plans without these entries, since spawned execution becomes the only mode.

  **Response (round 1):** Decision 4 rewritten: the create adapters ask for and record executor + implementation-reviewer profiles during the interview (design reviewer stays per-invocation); plans without entries fall back to the stated defaults at spawn time. plan-create adapters added to Scope In and to item 1's goal/validation.

  **Verification (round 1):** Verified: decision 4 defines creation-time selection, separates the design reviewer, and supplies legacy-plan defaults; Scope and item 1 assign the create-adapter changes.

- [x] **The global model rule contradicts the retained design-review exception.** [plan.md:27](plan.md:27) says the orchestrator's model no longer matters because it executes nothing, while decision 11 at [plan.md:33](plan.md:33) retains it as the design-review executor. Likewise, [breakdown.md:21](breakdown.md:21) forbids any protocol or adapter text saying the orchestrator doubles as executor, including the deliberately retained design-review behavior. Scope the new rule and exit criterion to implementation, and explicitly preserve the design review's check against its actual editor's model. Otherwise the shared protocol can authorize self-review or contradict the unchanged create adapters.

  **Response (round 1):** Decision 5 rewritten to scope the executor!=reviewer rule to the implementation loops and to state the design review keeps its existing orchestrator-as-editor rule. Item 1's exit criteria now scope the no-implementing claim to implementation loops with the exception explicitly preserved; a success criterion names the preserved exception.

  **Verification (round 1):** Verified: decision 5 and item 1 explicitly preserve the design-review editor/model rule while scoping the new rule to implementation.

- [x] **The primary success criterion is impossible as written.** [plan.md:91](plan.md:91) and [breakdown.md:37](breakdown.md:37) require zero implementation content in the orchestrator transcript, but decision 6 at [plan.md:28](plan.md:28) requires it to judge the diff and worklog, which contain implementation content. Define an observable boundary consistent with both decisions: no implementation authored by the orchestrator, template-only executor prompts, and a specified scope of artifact inspection. Human judgment cannot make the current contradictory criterion pass.

  **Response (round 1):** The criterion is now an authorship boundary: every file change comes from the executor process; the orchestrator's writes are limited to prompts, [session]/[decision]/evidence worklog entries, and reports; reading diffs/worklog is expected. Item 2's validation restated in the same terms.

  **Verification (round 1):** Verified: the success criterion and item 2 now test authorship and explicitly permit artifact reads.

- [x] **Resume and per-run overrides have no coherent session contract.** [plan.md:26](plan.md:26) permits an override on any invocation, but [plan.md:31](plan.md:31) requires the same executor session unless lost. Switching CLI profiles cannot resume the previous CLI's session. Neither [breakdown.md:7](breakdown.md:7) nor the risks specifies how an executor session is associated with an item and recovered when plan-item-review is invoked separately. Define where the item/session/profile association survives, how overrides interact with an active session, and what the fresh-session fallback reads to reconcile partial work. Template isolation alone does not establish the claimed recovery guarantee at [plan.md:82](plan.md:82).

  **Response (round 1):** Decisions 4 and 9 rewritten: a [session] worklog entry (item, seat, profile, session id) is appended at every spawn, so the association survives sessions; separately-invoked plan-item-review resumes from the item's latest [session] entry; a mid-item executor-LLM override forces a fresh spawn recorded as [decision]; a fresh session reconciles from tree + review file + worklog. [session] added to the ARTIFACTS.md contract in item 1.

  **Verification (round 1):** Verified: decisions 4 and 9 define persisted item/seat/profile/session records, fresh spawning on profile changes, and recovery from the tree, review file, and worklog; item 1 includes the artifact contract.

- [x] **Blockers and abnormal exits can fall into an endless pre-review retry loop.** [plan.md:28](plan.md:28) expects completion or a written blocker, and [plan.md:29](plan.md:29) unconditionally resumes on red. A permission/dependency wall will remain red until something external changes; a crash or urgent kill may leave neither a terminal entry nor a usable session ID. Define how the orchestrator distinguishes completion, actionable validation failure, unresolved blocker, and abnormal termination; when it stops for the human; and how repeated unsuccessful retries terminate. The urgent-stop behavior at [plan.md:30](plan.md:30) also needs to ensure child work has stopped before another executor can write to the same tree. Add these outcomes to the exercise's required evidence.

  **Response (round 1):** Decision 6 now defines the exit taxonomy (implemented / blocked / abnormal, with the orchestrator writing the [blocker] on an abnormal exit and blockers never auto-resumed); decision 7 caps validation-red resumes at three before escalation; decision 8 requires confirming the killed process exited before anything else writes. The exercise's required evidence (breakdown item 2) now includes the taxonomy paths, forced where natural runs do not produce them.

  **Verification (round 1):** Partially addressed: decisions 6 and 7 stop on blockers/abnormal exits and bound validation retries. The shutdown requirement at [plan.md:30](plan.md:30) still confirms only the executor process has exited. Its build/test subprocesses can survive that process and keep writing. Require stopping and confirming termination of the executor and its child work before permitting another writer; parent-process exit alone does not establish the requested boundary.

  **Response (round 2):** Decision 8 now requires killing the executor's entire process tree - the CLI and any build/test children - and confirming nothing from that tree survives before anything else may write.

  **Verification (round 2):** Verified: decision 8 now requires terminating the entire executor process tree and confirming no children survive before another writer proceeds. Together with decisions 6 and 7, this addresses the remaining shutdown and retry concerns.

- [x] **The validation gate lacks an evidence handoff and a rule for non-executable checks.** Decision 10 at [plan.md:32](plan.md:32) allows only document/file pointers, while decision 7 requires the executor to act on validation run by the orchestrator. Specify where the failing command and useful result are recorded before a template-only resume. Also define how manual or human-dependent validation is treated: [breakdown.md:18](breakdown.md:18) requires a manual read, and [breakdown.md:37](breakdown.md:37) requires human transcript judgment. These are neither shell exit codes nor failures an executor can necessarily fix; pending human validation must not automatically trigger another executor round.

  **Response (round 1):** Decision 7 defines the evidence handoff: failing command + trimmed output appended to the worklog, resume prompt points at that entry, template remains the only channel. Validation lines marked human: never trigger executor rounds; both breakdown items now mark their human judgments as human: lines.

  **Verification (round 1):** Verified: decision 7 specifies worklog evidence and template-only resume pointers, with human checks routed to a pause rather than automatic retries; both breakdown items mark their human checks.

- [x] **Executor progress uses a tag reserved for committed delivery.** [plan.md:45](plan.md:45) directs the executor to append `[done]` entries even though it cannot commit and has not passed review. The existing contract in [ARTIFACTS.md:73](../../../home/.config/plan-skills/ARTIFACTS.md:73) defines `[done]` as an item having landed, with commits and verification. Choose an unambiguous pre-review completion/progress convention and include any necessary artifact-contract change in item 1. Otherwise the file-based audit trail cannot distinguish executor completion from delivered work.

  **Response (round 1):** New worklog type [implemented] for pre-review executor completion; [done] keeps its committed meaning. ARTIFACTS.md contract change added to Scope In and item 1, with a grep validation.

  **Verification (round 1):** Verified: Scope and item 1 assign the new [implemented] artifact type and preserve [done] for committed delivery.

- [x] **The self-hosted rollout has no activation boundary.** [plan.md:34](plan.md:34) requires this change to use the lifecycle it is replacing, while [breakdown.md:14](breakdown.md:14) treats one coherent source change as sufficient to prevent runtime skew. The adapters read `~/.config/plan-skills`, and [AGENTS.md](../../../AGENTS.md) explicitly says this checkout may be live-linked or store-managed. Define how item 1 is executed/reviewed during the transition, and make item 2 depend on verifying that its invoked adapters and core resolve to the updated version. A single source change neither activates a store-managed installation nor protects a live session from observing partly edited files.

  **Verification (round 1):** Partially addressed: [breakdown.md:22](breakdown.md:22) defines the old-protocol bootstrap, and [breakdown.md:38](breakdown.md:38) checks core activation. The activation check still omits the invoked adapters under ~/.claude/skills and ~/.codex/prompts. Checking ~/.config/plan-skills alone can pass while an old adapter still instructs the orchestrator to implement. Extend the pre-exercise check to the adapters actually invoked, including starting a session that loads their updated content where needed.

  **Response (round 2):** Item 2's activation check now covers the core AND all six invoked adapters (claude skills and codex prompts), and requires the exercise's orchestrating session to be started fresh so it loads the updated adapter text.

  **Verification (round 2):** Verified: item 2 now checks the core and all six adapters against item 1's updated content and requires a fresh orchestrating session, completing the activation boundary alongside item 1's bootstrap rule.

- [x] **The specified checks do not prove several claimed outcomes.** The Ruby command at [breakdown.md:18](breakdown.md:18) reads only `ARGV[0]`, so it never validates the second skill file. The exercise at [breakdown.md:27](breakdown.md:27) forces a validation failure but does not require an under-context blocker/doc repair, despite [plan.md:76](plan.md:76) claiming it deliberately probes that mitigation. Its exit criteria also permit all success criteria to pass without an ADDRESS round if the initial review is clean, although its goal requires same-session ADDRESS. Correct the file validation and require explicit evidence for the claimed under-context recovery and ADDRESS resume, with a defined way to exercise them when the real item does not naturally trigger them.

  **Response (round 1):** Transition: item 1 is explicitly the last orchestrator-executed item, run under the old protocol in one session that does not re-read the core mid-loop (recorded as a [decision]); item 2 begins by verifying ~/.config/plan-skills resolves to the updated content, and the self-hosted transition is a named risk. The ruby check iterates ARGV. The exercise's goal now forces the under-context probe (deliberately omitted fact -> [blocker] -> doc repair -> resume) and defines the ADDRESS-resume fallback explicitly.

  **Verification (round 1):** Verified: the Ruby check iterates all arguments, item 2 requires the omitted-fact blocker/repair probe, and the revised success criteria explicitly allow validation-red resume evidence when review is clean. This is a narrower, honestly recorded claim about resume mechanics, not proof that ADDRESS was exercised.

- [x] **Exit classification does not distinguish this invocation from worklog history.** The new taxonomy at [plan.md:28](plan.md:28) tests whether `[implemented]` or `[blocker]` entries are present, but the worklog is append-only and item 2 explicitly requires blocker -> repair -> resume at [breakdown.md:48](breakdown.md:48). A successful resumed invocation will therefore coexist with an earlier blocker; a resumed invocation that crashes can coexist with an earlier implementation-complete entry. Define classification using only terminal evidence from the current item/seat invocation, with a boundary for every resume as well as every fresh spawn and a rule for conflicting or absent terminal evidence. Add a check that an old entry cannot classify a later invocation as successful or blocked.

  **Response (round 1):** Decision 6 now classifies per invocation: the [session] entry - appended at every spawn AND every resume (decision 9) - is the boundary, only terminal entries after the latest boundary count, no terminal entry after it is abnormal, both kinds after it is a conflict treated as blocked, and older entries are history that never classifies a later invocation.

  **Verification (round 2):** Verified: decision 6 establishes a boundary for every spawn and resume, ignores earlier terminal entries, treats missing current entries as abnormal, and treats conflicting current entries as blocked. Item 2's required blocker/repair/resume and validation-red/resume paths exercise classification across retained history.
