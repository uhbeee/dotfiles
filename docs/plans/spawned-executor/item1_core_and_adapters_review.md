- [x] **Make the initial session record executable without losing the exit boundary.** `home/.claude/skills/plan-implement/SKILL.md:31` and `home/.codex/prompts/plan-implement.md:20` require the complete `[session]` entry before spawning, but `home/.config/plan-skills/PROTOCOL.md:82` requires a session id obtained from CLI output (`:187`, `:204`), which for the documented Claude recipe arrives at completion. There is no prescribed way to record the id later without editing the append-only log or creating a new boundary after the terminal entry. Specify how the pre-launch boundary and eventual session id are associated, including termination before an id is available. Decisions 6 and 9 require both reliable classification and a durable resume pointer.

  **Response (round 1):** [session] entries are now a pair (PROTOCOL.md executor-seat section, ARTIFACTS.md, both implement adapters, both item-review adapters): a boundary entry appended immediately before the spawn (item, seat, profile, invocation number - the classification boundary) and an id entry appended once the CLI reports the session id (the resume pointer; codex's arrives on the first stdout line, claude's in the JSON result at exit). A boundary with no id entry has no resumable session and recovery is a fresh spawn - which covers termination before an id exists.

- [x] **Make recovery prompts self-contained and read the recorded state.** `home/.config/plan-skills/PROTOCOL.md:114` allows a fresh spawn after session loss/profile change, but `home/.config/plan-skills/ROLES.md:116` and `:126` refer to the executor template's rules without supplying them or instructing the fresh seat to read them. The ADDRESS prompt also omits the work item and explicit `[implemented]` completion rule. Conversely, the initial executor template does not tell a replacement seat to reconcile partial work or address existing review items. Define which complete template is used for fresh recovery and require reading the relevant worklog decisions and existing artifacts. This also supplies the missing template-only route for the human steering promised at `PROTOCOL.md:107`; none of the current templates instructs the executor to read that steering. Decisions 8-10 require recovery and steering to work without session memory or added chat context.

  **Response (round 1):** All three executor templates are self-contained now. The full executor template opens with a reconciliation rule (read worklog tail + review file; earlier entries, open items, and partial tree work are yours to continue, never redo; [decision] entries bind you) and explicitly serves fresh replacement seats; executor-address and executor-resume-validation each start with the read-the-worklog-tail steering rule and carry the full rule set inline ([blocker] at walls, terminal-entry requirement, human: exception, never close/edit/commit) instead of citing the executor template. PROTOCOL.md's Resume-vs-fresh names the full template as the fresh-recovery prompt.

- [x] **Gate VERIFY on the ADDRESS invocation's outcome.** `home/.config/plan-skills/PROTOCOL.md:125` proceeds directly from ADDRESS to VERIFY, while exit classification and validation are presented under "The executor seat (plan-implement)" at `:76`. Neither item-review adapter explicitly inserts these checks. Thus a standalone item-review can spend a reviewer round after an executor blocker, abnormal exit, or failing validation. Explicitly apply the per-invocation boundary, exit classification, human escalation, and validation/evidence-resume gate after ADDRESS, before VERIFY, as required by decisions 6 and 7.

  **Response (round 1):** The loop gains an explicit GATE step between ADDRESS and VERIFY (PROTOCOL.md): classify the ADDRESS exit per the executor-seat section, blocked/abnormal to the human, and re-run the item's validation line when the fixes changed the work, red following the validation gate. Both item-review adapters restate it, and the executor-address template now requires a terminal [implemented]/[blocker] entry per round so the classification has evidence (PROTOCOL.md 'Every invocation terminates in the worklog').

- [x] **Tell the executor to defer human validation.** `home/.config/plan-skills/ROLES.md:99` makes all validation and exit criteria binding and requires a blocker at any wall, but never passes along the `human:` exception from `PROTOCOL.md:105`. The spawned seat receives only this template and the target plan docs, so an item with human validation can become blocked waiting for a check it cannot perform instead of returning `[implemented]` for the orchestrator's human pause. Add the exception to the executor contract and preserve it on resumes, per decision 7.

  **Response (round 1):** All three templates carry the human: exception: satisfy everything else, note pending human checks in the [implemented] entry, never block on them.

- [x] **Align the adapter retry cap with the approved resume cap.** `home/.claude/skills/plan-implement/SKILL.md:37` and `home/.codex/prompts/plan-implement.md:26` escalate after "three reds," whereas decision 7 and `home/.config/plan-skills/PROTOCOL.md:103` specify three validation-red resumes without green. Counting the initial failed validation as a red can escalate after only two repair attempts. Use the same resume-based counting rule in both adapters and the core so the atomic change does not introduce conflicting loop limits.

  **Response (round 1):** Both implement adapters now say 'three validation-red resumes on one item without green escalates', matching decision 7 and the core - resume-based counting, so the initial red does not consume a repair attempt.

- [x] **Keep the new GATE step out of design review.** `home/.config/plan-skills/PROTOCOL.md:140` adds exit classification and implementation validation to the shared loop, but the design-review section at `:170` still says to run the same loop with only the conformance gate excluded (`:178`). After the orchestrator edits a draft plan, there is no spawned ADDRESS invocation or terminal `[implemented]` entry to classify, and the draft breakdown's validation commands describe work that has not been implemented. Explicitly exempt design review from the new GATE step so it proceeds from plan edits to reviewer verification as before. Decisions 5 and 11 and the scope exclusion require the design-review loop to remain unchanged. Also repair the duplicated step 5 and the numeric conformance reference while updating this flow.

  **Response (round 2):** The design-review section now explicitly exempts itself from the executor seat's machinery - no spawned seat, no [session] entries, no exit classification, no GATE step - proceeding from plan edits straight to VERIFY, with the rationale stated (no ADDRESS invocation to classify; draft validation lines describe unimplemented work). The loop's numbering is repaired (5/6/7) and the design-review section's conformance reference updated to step 7.

  **Verified (round 2):** The design-review exception explicitly skips executor classification and GATE, preserves reviewer verification and human pauses, and correctly excludes conformance step 7. No new issue found in this fix.
